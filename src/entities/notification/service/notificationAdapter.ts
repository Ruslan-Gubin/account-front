// import { useActiveNotification } from '../application'
import type { NotificationService } from '../application/port'

// const { activeNotification } = useActiveNotification()

export const notificationAdapter: NotificationService = { 

}